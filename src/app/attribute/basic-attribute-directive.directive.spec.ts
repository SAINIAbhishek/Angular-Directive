import { BasicAttributeDirectiveDirective } from './basic-attribute-directive.directive';

describe('BasicAttributeDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new BasicAttributeDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
