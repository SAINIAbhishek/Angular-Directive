import { RendererAttributeDirectiveDirective } from './renderer-attribute-directive.directive';

describe('RendererAttributeDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new RendererAttributeDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
