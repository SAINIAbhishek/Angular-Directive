import { HostlistenerAttributeDirectiveDirective } from './hostlistener-attribute-directive.directive';

describe('HostlistenerAttributeDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new HostlistenerAttributeDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
