import { OppositeNgifDirectiveDirective } from './opposite-ngif-directive.directive';

describe('OppositeNgifDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new OppositeNgifDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
